name: example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0'
dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  bloc: ^8.1.1
  flutter_bloc: ^8.1.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  able:
    path: ../../able
  build_runner: ^2.4.6
  built_value_generator: ^8.6.1
flutter:
  uses-material-design: true