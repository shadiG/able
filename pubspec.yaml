name: able
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: '>=3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  built_value: ^8.6.1
  built_collection: ^5.1.1
  rxdart: ^0.27.7
  bloc: ^8.1.1
  flutter_bloc: ^8.1.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.5.0
  build_runner: ^2.4.6
