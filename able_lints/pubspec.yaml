name: able_lints
description: Analyzer plugin that flags the Able mistakes reviews catch most often.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  analysis_server_plugin: ^0.3.23
  analyzer: ^14.0.0

dev_dependencies:
  analyzer_testing: ^0.4.2
  lints: ^6.0.0
  test: ^1.25.0
  test_reflective_loader: ^0.4.0
