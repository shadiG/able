# Counter

The smallest `able` example: `CounterCubit` holds one `Fetchable<int>`, and a `FetchableWidget` renders it.

```sh
flutter pub get
flutter run
flutter test
```

For a full feature (business and view cubits, derived state, expected errors, tests), see [country_listing](../country_listing/).
