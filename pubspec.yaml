name: able
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  built_value: ^8.0.5
  built_collection: ^5.0.0
  rxdart: ^0.27.7
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: